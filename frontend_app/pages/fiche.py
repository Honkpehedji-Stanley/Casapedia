"""Fiche territoriale — drill-down sur un territoire (Sprint 3).

Cible : KPI locaux + courbes + prédiction ML (parquet MinIO via lib.connections
.get_minio()) + derniers avis (MongoDB reviews_clean).
"""
import streamlit as st

st.title("📍 Fiche territoriale")
st.info("🚧 **Sprint 3** — KPI locaux, prédiction ML et avis récents. À venir.")
